# eslint-plugin-no-switch-true
Prevent switch(true) usage

# .eslintrc
```json
{
  "...": {},
  "plugins": [
    "no-switch-true"
  ],
  "rules": {
    "no-switch-true/no-switch-true": "error"
  }
}

